# ============================================================
# SKILLA Backend — Environment Variables
# Copia in .env e compila i valori reali.
# ============================================================

NODE_ENV=development
PORT=3001

# Supabase (usa SERVICE ROLE KEY — solo backend, mai esposta al frontend!)
SUPABASE_URL=https://tuo-progetto.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...la-tua-service-role-key

# Frontend URL (per CORS)
FRONTEND_URL=http://localhost:3000
